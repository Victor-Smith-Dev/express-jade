extends ../../layout.jade
block contenido
	div(class="img-container center-block")
		h1= image.title
		img( src="/static/img/#{image._id}.#{image.extension}" class="main-img")
		if ( typeof image.owner != "undefined")
			p Por #{ image.owner.email }